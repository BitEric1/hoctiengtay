import React from "react";
import Link from 'next/link';

// Giả định bạn đã cài đặt lucide-react hoặc một thư viện icon tương tự
// Nếu không, bạn có thể thay thế bằng SVG inline
import { Video } from 'lucide-react';
const page = () => {
  return (
    <div className="max-w-screen-xl mx-auto mt-20 h-screen">
     
    </div>
  );
};

export default page;
